= content_for :page_header do
  %h1 Users

- if @users.present?
  %table.table-bordered.table-striped
    %tr
      %th Username

    - @users.each do |user|
      %tr
        %td= user.username
- else
  %p No users created, yet

= link_to 'New User', new_user_path, class: 'btn btn-primary'
